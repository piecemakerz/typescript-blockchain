const name = "piecemakerz",
  age = 24,
  gender = "male";

const sayHi = (name, age, gender?) => {
  console.log(`Hi, I'm ${name} and age of ${age}, whose gender is ${gender}`);
};

sayHi(name, age);
export {};
